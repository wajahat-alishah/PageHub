
"use client";

// This file is a workaround to make the (main) route group work correctly.
// It ensures that the root of the (main) group is handled by the page.tsx inside it.
import HomePage from '../page';
export default HomePage;
